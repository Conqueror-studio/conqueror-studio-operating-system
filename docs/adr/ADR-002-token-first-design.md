# ADR-002: Use token-first design

**Status:** Accepted. Design values must originate in CDS primitives and semantic tokens, not arbitrary local values. This keeps Figma and implementation coherent and permits deliberate evolution.
